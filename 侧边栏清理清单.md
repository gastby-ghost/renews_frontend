# Art Design Pro 侧边栏清理清单

## 保留的功能模块

根据要求，以下模块**需要保留**：

### ✅ 素材库（Material）

- 路径: `/material`
- 菜单项:
  - 素材搜索
  - 素材管理

### ✅ AI创作（DocumentGeneration）

- 路径: `/document-generation`
- 菜单项:
  - 项目列表
  - 选题策划
  - 大纲
  - 正文

---

## 需要移除的功能模块

以下模块**可以移除**（除了保留的"素材库"和"AI创作"外）：

### ❌ 1. Dashboard（仪表盘）

**路由**: `/dashboard` **移除的文件**:

#### 视图文件

```
src/views/dashboard/
├── analysis/
│   ├── index.vue
│   └── widget/
│       ├── SalesMappingByCountry.vue
│       ├── TopProducts.vue
│       ├── TotalRevenue.vue
│       ├── CustomerSatisfaction.vue
│       ├── VisitorInsights.vue
│       ├── TargetVsReality.vue
│       ├── TodaySales.vue
│       └── VolumeServiceLevel.vue
├── console/
│   ├── index.vue
│   └── widget/
│       ├── SalesOverview.vue
│       ├── TodoList.vue
│       ├── NewUser.vue
│       ├── Dynamic.vue
│       ├── CardList.vue
│       ├── AboutProject.vue
│       └── ActiveUser.vue
└── ecommerce/
    ├── index.vue
    └── widget/
        ├── HotProductsList.vue
        ├── AnnualSales.vue
        ├── SalesClassification.vue
        ├── SalesTrend.vue
        ├── TotalProducts.vue
        ├── RecentTransaction.vue
        ├── TransactionList.vue
        ├── TotalOrderVolume.vue
        ├── Banner.vue
        ├── ProductSales.vue
        ├── SalesGrowth.vue
        ├── CartConversionRate.vue
        └── HotCommodity.vue
```

### ❌ 2. Template（模板）

**路由**: `/template` **移除的文件**:

```
src/views/template/
├── cards/index.vue
├── banners/index.vue
├── charts/index.vue
├── map/index.vue
├── chat/index.vue
├── calendar/index.vue
└── pricing/index.vue
```

### ❌ 3. Widgets（小部件）

**路由**: `/widgets` **移除的文件**:

```
src/views/widgets/
├── icon-list/index.vue
├── icon-selector/index.vue
├── image-crop/index.vue
├── excel/index.vue
├── video/index.vue
├── count-to/index.vue
├── wang-editor/index.vue
├── watermark/index.vue
├── context-menu/index.vue
├── qrcode/index.vue
├── drag/index.vue
├── text-scroll/index.vue
└── fireworks/index.vue
```

### ❌ 4. Examples（示例）

**路由**: `/examples` **移除的文件**:

```
src/views/examples/
├── tabs/index.vue
├── tables/
│   ├── index.vue
│   ├── tree.vue
│   └── basic.vue
└── forms/
    └── search-bar.vue
```

### ❌ 5. System（系统管理）

**路由**: `/system` **移除的文件**:

```
src/views/system/
├── user/
│   ├── index.vue
│   ├── modules/
│   │   ├── user-dialog.vue
│   │   └── user-search.vue
├── user-center/index.vue
├── role/index.vue
├── menu/index.vue
└── nested/
    ├── menu1/index.vue
    ├── menu2/index.vue
    └── menu3/
        ├── index.vue
        └── menu3-2/index.vue
```

### ❌ 6. Article（文章管理）

**路由**: `/article` **移除的文件**:

```
src/views/article/
├── list/index.vue
├── detail/index.vue
├── comment/index.vue
└── publish/index.vue
```

### ❌ 7. Result（结果页）

**路由**: `/result` **移除的文件**:

```
src/views/result/
├── success/index.vue
└── fail/index.vue
```

### ❌ 8. Exception（异常页）

**路由**: `/exception` **移除的文件**:

```
src/views/exception/
├── 403/index.vue
├── 404/index.vue
└── 500/index.vue
```

### ❌ 9. Safeguard（保障）

**路由**: `/safeguard` **移除的文件**:

```
src/views/safeguard/
└── server/index.vue
```

### ❌ 10. ChangeLog（更新日志）

**路由**: `/change/log` **移除的文件**:

```
src/views/change/
└── log/index.vue
```

---

## 路由配置清理

### 移除 asyncRoutes.ts 中的路由定义

从 `/home/shan/art-design-pro/src/router/routes/asyncRoutes.ts` 文件中移除以下路由配置：

1. **Dashboard** (第31-70行)
2. **Template** (第72-145行)
3. **Widgets** (第147-286行)
4. **Examples** (第288-344行)
5. **System** (第346-498行)
6. **Article** (第500-565行)
7. **Result** (第567-594行)
8. **Exception** (第596-635行)
9. **Safeguard** (第638-657行)
10. **ChangeLog** (第672-681行)

**保留**:

- **Material** (第683-722行)
- **DocumentGeneration** (第724-777行)

### 清理 routesAlias.ts 中的别名

从 `/home/shan/art-design-pro/src/router/routesAlias.ts` 文件中移除以下枚举值：

**移除的别名** (保留的跳过):

- `Exception403`
- `Exception404`
- `Exception500`
- `Success`
- `Fail`
- `Dashboard`
- `Analysis`
- `Ecommerce`
- `IconList`
- `IconSelector`
- `ImageCrop`
- `Excel`
- `Video`
- `CountTo`
- `WangEditor`
- `Watermark`
- `ContextMenu`
- `Qrcode`
- `Drag`
- `TextScroll`
- `Fireworks`
- `Chat`
- `Cards`
- `Banners`
- `Charts`
- `Map`
- `Calendar`
- `Pricing`
- `ArticleList`
- `ArticleDetail`
- `Comment`
- `ArticlePublish`
- `User`
- `Role`
- `UserCenter`
- `Menu`
- `NestedMenu1`
- `NestedMenu21`
- `NestedMenu31`
- `NestedMenu321`
- `Server`
- `ChangeLog`
- `ExamplesTabs`
- `ExamplesTablesBasic`
- `ExamplesTables`
- `ExamplesTablesTree`
- `ExamplesSearchBar`

**保留的别名**:

- `MaterialLibrary`
- `MaterialManagement`
- `MaterialSearch`
- `DocumentGeneration`
- `DocumentTopicSelection`
- `DocumentOutline`
- `DocumentContent`

---

## 开发测试相关文件

### ❌ API测试页面

```
src/views/dev/
└── api-test/index.vue
```

---

## 静态路由清理

### staticRoutes.ts

从 `/home/shan/art-design-pro/src/router/routes/staticRoutes.ts` 文件中移除：

1. **UserPreferences** (第45-49行) - 用户偏好设置（可选，如不需要认证相关功能）
2. **/403** (第51-55行) - 403页面
3. **/500** (第63-67行) - 500页面
4. **/outside** (第69-81行) - 外部链接（如果不需要）

**保留**:

- 登录相关路由（Login, Register, ForgetPassword, VerifyEmail）
- 404页面（Exception404）

---

## 国际化文件清理

需要从以下语言包文件中移除对应的翻译键值：

### /home/shan/art-design-pro/src/locales/

- `zh-CN.ts`
- `en-US.ts`

**移除的键值前缀**:

- `menus.dashboard.*`
- `menus.template.*`
- `menus.widgets.*`
- `menus.examples.*`
- `menus.system.*`
- `menus.article.*`
- `menus.result.*`
- `menus.exception.*`
- `menus.safeguard.*`
- `menus.plan.log`
- `menus.help.document`

**保留的键值**:

- `menus.material.*`
- `menus.documentGeneration.*`

---

## 服务层文件清理

### 可选移除的 API 服务文件

如果确认不再使用，可以考虑移除：

```
src/services/modules/
├── ai/                 # 保留（AI创作使用）
├── auth/               # 保留（认证需要）
├── document/           # 保留（AI创作使用）
├── material/           # 保留（素材库使用）
├── project/            # 保留（AI创作使用）
├── research-brief/     # 保留（AI创作使用）
├── title/              # 保留（AI创作使用）
├── user/               # 可以移除
├── validation/         # 保留（通用）
├── auth/               # 保留
├── outline/            # 保留（AI创作使用）
└── index.ts            # 更新导出
```

**注意**: 请谨慎删除服务文件，确保没有其他地方引用。

---

## 状态管理清理

### Store 模块

```
src/store/modules/
├── user.ts                    # 可以移除（如果不需要用户管理）
├── setting.ts                 # 保留（系统设置）
├── worktab.ts                 # 保留（工作标签）
└── index.ts                   # 更新导出
```

**注意**: 菜单状态管理 (`menu.ts`) 保留，因为它用于动态菜单控制。

---

## 组件清理

### 自定义组件

大部分业务组件都与保留功能相关，但以下组件可以清理：

```
src/components/custom/
├── TitleCard.vue              # 需要检查使用情况
```

### 核心组件

保留所有核心UI组件，因为其他功能可能使用到。

---

## 工具函数清理

### 保留

所有工具函数都保留，因为可能是通用工具：

- HTTP客户端
- 存储工具
- 轮询工具
- 认证工具
- 主题工具
- 验证工具

---

## 总结

### 清理步骤建议

1. **备份**: 在清理前务必备份项目
2. **移除视图文件**: 按照上述列表删除对应的 `src/views/` 下的文件
3. **更新路由配置**: 清理 `asyncRoutes.ts` 和 `routesAlias.ts`
4. **更新语言包**: 清理国际化文件中的翻译键值
5. **清理静态路由**: 如需要，清理 `staticRoutes.ts`
6. **验证构建**: 运行 `pnpm build` 确保无错误
7. **测试功能**: 测试保留的"素材库"和"AI创作"功能是否正常

### 清理后的侧边栏菜单

清理后，侧边栏将只包含以下两个一级菜单：

1. **📦 素材库**

   - 素材搜索
   - 素材管理

2. **🤖 AI创作**
   - 项目列表
   - 选题策划
   - 大纲
   - 正文

### 预计效果

- **代码量减少**: 约 60-70% 的视图文件
- **构建速度提升**: 减少编译文件数量
- **维护成本降低**: 专注于核心功能
- **用户体验优化**: 界面更简洁，导航更清晰

---

**⚠️ 注意事项**:

1. 某些工具函数和组件可能在多个地方使用，删除前请确保没有引用
2. 建议分阶段清理，先删除视图文件，再逐步清理路由和配置
3. 如有疑问，请先注释代码而不是直接删除，以便回滚
4. 清理完成后，记得更新文档

**维护者**: 开发团队 **创建日期**: 2025-11-09
